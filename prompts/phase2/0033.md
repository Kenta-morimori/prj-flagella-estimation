# 0033: hook / flagella projection の切り分けと，論文準拠の初期べん毛生成・初期幾何 logging・torsion force 検証の整備

## 対象
- Repo: `Kenta-morimori/prj-flagella-estimation`
- Base branch: `main`
- このタスクでは **main から新規ブランチを切って作業**すること
- 対応 Issue: #33
- 想定タイトル: `[Phase2] hook, flagellaのprojection廃止`

### 作業上の原則
- **不明点や曖昧な点・詰めるべき要件が見られる場合は事前に質問すること**
- 独断でタスク範囲を拡張しないこと
- 各フェーズの結論と根拠をログ・summary・notes のいずれかに残すこと

---

## 背景

直前の PR #32 / Issue #29 では，body-only 条件を切り出して安定性を確認した。

- `projection.enable_body_rigid_projection` を追加し，body rigid projection を ON/OFF 可能にした
- body 側面長方形に **両対角 spring** を追加した
- `n_flagella=0` では，`projection.enable_body_rigid_projection=false` でも body は安定した
- 一方で `n_flagella=3` では body 崩壊が起きうるため，主因は body 単体ではなく **hook / flagella が body に返す負荷**の可能性が高い

このため，次の切り分け対象として **hook / flagella projection 側** を扱った。

### 追加で今回分かったこと
PR #34 の Stage 1 (`n_flagella=1`, `motor.torque_Nm=0`) の 2x2 比較では，

- hook OFF / flagella OFF
- hook OFF / flagella ON
- hook ON / flagella OFF
- hook ON / flagella ON

の **全条件で形状崩壊**が生じた。

その後，`paper_table1`・`n_flagella=1`・`motor.torque_Nm=0`・all projection OFF で再検証したところ，

- 初期 bend / torsion / hook angle は target と実質一致
- `paper_table1` と all projection OFF は正しく反映
- `fd_eps_over_b=0.1` では step 0 から崩壊
- `fd_eps_over_b=1e-3` および `1e-4` では，菌体・べん毛とも静止平衡をほぼ維持

であった。

したがって，projection の有無そのものや，paper mode における初期 shape mismatch は主因ではない可能性が高い。  
現時点で有力な解釈は，**torsion force 実装側，特に有限差分幅 `fd_eps_m` が大きすぎることで，初期平衡状態でも過大な torsion force が出ていた**ことである。

現時点では，
- **Phase A（`fd_eps_m` 検証）は完了済み**
- **`fd_eps_over_b=1e-3` を current candidate とする**
- **次の必須検証は `motor ON, n_flagella=1`**
- `flag_torsion_stiffness_scale` / `flag_bend_stiffness_scale` の扱いに関する最終結論はその後

とする。

---

## 現 `main` / 現ブランチの把握事項（前提）

実装前に必ず現コードを読んで確認すること。

### 1. 現在 config 化されている projection は body 側のみ
- `projection.enable_body_rigid_projection` は存在する
- hook / flagella projection を個別に切る config はまだない

### 2. `step()` の最後で hook / flagella 側 projection が常時走る
現 `main` では，body rigid projection の後に hook / flagella 側 projection が必ず適用される。

### 3. hook / flagella projection の中身は少なくとも以下に分かれている
- hook length の補正
- basal link direction の補正
- flagella chain length の補正
- flagella template の補正

### 4. hook の現在の物理定義
- `attach bead - first flag bead` は spring pair に入っている
- hook は `(attach, first, second)` の triplet で表されている
- motor triplet も同じ triplet を使っている

### 5. 初期化時の幾何
- `attach -> first` は body layer centroid から見た radial 方向に短く置かれている
- `first -> second` の基底接線は rear direction に概ね揃えられている
- これは **初期条件** であり，時間発展中もその方向を hard に固定すべきとは限らない

### 6. 現状の問題点
- 旧来の `legacy_radius_pitch` では，初期べん毛形状と target bend / torsion が独立であり，初期 mismatch を生みうる
- 一方，`paper_table1` では初期 shape mismatch は大きく改善された
- 残る主因候補は torsion force 実装側，特に `fd_eps_m`
- 現在の Phase A では，`fd_eps_over_b=0.1` で崩壊し，`1e-3` と `1e-4` で静止平衡が保たれた
- engine 内には `flag_bend_stiffness_scale = 300`，`flag_torsion_stiffness_scale = 300`，`body_stiffness_scale = 50` があり，これらは論文の一次入力ではなく repo 独自の補助係数である可能性が高い
- ただし，現時点では **scale 変更を先に行うのではなく，まず `motor ON, n_flagella=1` で `fd_eps_over_b=1e-3` を検証**すべきである

---

## このタスクの目的

### 第1目標
`motor.torque_Nm=0` で，**hook / flagella projection を切っても**，body / hook / flagella が直ちに崩壊しないことを確認できる状態にする。

### 本PRの最終目標
`motor.torque_Nm>0` でも，projection を切った条件で **body / hook / flagella が即時の凝集・発散を起こさないか** を比較・診断できる状態にする。

### このPRで達成しないこと
- 3本べん毛が菌体後方で束化し，推進する最終 run 状態の達成
- body-only モデルの再設計
- 無関係な refactor
- hidden projection / hidden correction の再導入
- 初期 mismatch を stiffness 緩和だけで見かけ上隠すこと
- `flag_torsion_stiffness_scale` を根拠なく即変更すること

---

## 今回の設計方針（重要）

### 方針 1: hook は「角度固定」ではなく「距離中心」で扱う
論文モデル寄りに，hook については以下を採ること。

- **常時の角度固定は行わない**
- `attach-first` の距離は **projection ではなく spring** を主拘束とする
- hook angle は，body 交差を防ぐための **one-sided な hook 制約**として扱うのは可
- `basal link direction` の hard projection は，論文より強い拘束の可能性があるため，個別に ON/OFF 可能にする

### 方針 2: hook と flagella の projection はまとめてではなく分解する
最低限，以下を個別に ON/OFF 可能にすること。

- `projection.enable_hook_length_projection`
- `projection.enable_basal_link_direction_projection`
- `projection.enable_flagella_chain_length_projection`
- `projection.enable_flagella_template_projection`

既定値は **すべて `true`** とし，現状互換を保つこと。

### 方針 3: 切り分けは段階的に行う
`hook OFF / flagella ON` だけで因果を断定しないこと。  
少なくとも以下の比較ができるようにする。

1. hook ON / flagella ON
2. hook OFF / flagella ON
3. hook ON / flagella OFF
4. hook OFF / flagella OFF

ここで
- hook 系 = `hook length` + `basal link direction`
- flagella 系 = `chain length` + `template`

とする。

### 方針 4: 初期べん毛は「論文の離散モデル量」を正とする
初期べん毛形状は，`helix_init.radius_over_b` と `helix_init.pitch_over_b` を一次入力とするのではなく，**論文 Table 1 に対応する離散モデル量**を source of truth とすること。

少なくとも以下を一次入力候補として扱う。

- `bond_L_over_b`
- `potentials.bend.theta0_deg.normal`
- `potentials.torsion.phi0_deg.normal`
- `n_beads_per_flagellum`
- `n_flagella`

`radius_over_b` と `pitch_over_b` は，必要なら **派生値として計算・記録**する。  
paper 準拠 mode では，`radius/pitch` を手入力で同時指定して source of truth を二重化しないこと。

### 方針 5: 初期状態の情報を log / summary に明示する
実長さの解釈や bead 数の妥当性を後から確認できるよう，初期状態について以下を必ず記録すること。

- 各べん毛の bead 数
- 各べん毛の contour length / end-to-end length
- `attach-first` 長さ
- flagella bond 長さ統計
- 初期 hook angle
- 初期 bend / torsion の実測値と target 値との差
- 派生された helix radius / pitch
- 初期 tangent と rear direction の角度
- 使用した init mode と source-of-truth 項目

### 方針 6: torsion force の問題はフェーズを分けて扱う
このタスクでは，以下の順で扱うこと。

#### Phase A（完了済み）
- **`fd_eps_m` を 100〜1000 倍小さくする sweep** を行った
- `paper_table1`・`n_flagella=1`・`motor.torque_Nm=0`・all projection OFF で再検証した
- `fd_eps_over_b=0.1` では崩壊
- `fd_eps_over_b=1e-3` および `1e-4` では静止平衡を維持
- 現時点の current candidate は **`fd_eps_over_b=1e-3`**

#### Phase B（次の必須検証）
- **`paper_table1`・`n_flagella=1`・`motor.torque_Nm>0`・all projection OFF・`fd_eps_over_b=1e-3`** を最優先で検証する
- この段階では，まず `fd_eps_m` 修正だけでどこまで保てるかを見る
- 不明点や結果の解釈が曖昧な場合は，ここで事前に質問すること

#### Phase C（条件付き）
- **Phase B でなお torsion force が異常に大きい，または `motor ON` で不安定性が残る場合に限り**，
  - `flag_torsion_stiffness_scale`
  - `flag_bend_stiffness_scale`
  の扱いを検討してよい
- ただし，scale 変更は「論文準拠 mode の default」を即変えるのではなく，
  - debug / legacy 専用に留める
  - paper mode では 1.0 を既定とすべきか
  - 完全に廃止すべきか
  まで含めて**結論を出す対象**とする

### 方針 7: 追加 scale は論文値ではない前提で扱う
- `flag_bend_stiffness_scale`
- `flag_torsion_stiffness_scale`
- `body_stiffness_scale`

は論文の一次入力そのものではなく，repo 独自の補助係数とみなすこと。  
したがって，paper mode ではその使用可否・既定値を慎重に扱うこと。

---

## 実装要件

## A. config / params の追加

`ProjectionParams` に以下を追加すること。

- `enable_hook_length_projection: bool = True`
- `enable_basal_link_direction_projection: bool = True`
- `enable_flagella_chain_length_projection: bool = True`
- `enable_flagella_template_projection: bool = True`

必要箇所:
- `src/sim_swim/sim/params.py`
- config YAML
- CLI override
- 必要なら summary / manifest 系の出力

既存の
- `projection.enable_body_rigid_projection`
はそのまま維持すること。

---

## B. engine の projection 適用を分解する

現状の一括処理を整理し，どの projection が有効かで分岐させること。

### 必須
- hook length projection を単独で切れること
- basal link direction projection を単独で切れること
- flagella chain length projection を単独で切れること
- flagella template projection を単独で切れること

### 禁止
- projection OFF のときに，別名の hidden correction を裏で走らせること
- OFF なのに内部で同等の位置補正を行うこと
- config 名だけ追加して実際には bypass されないこと

---

## C. diagnostics を既存 CSV に追加する

新規 CSV を増やすより，既存の `step_summary.csv` や既存 diagnostics に列追加する方針で進めること。  
ただし既存ファイル名が不適切なら修正してよい。その場合は理由を明記すること。

### 最低限追加したい列
projection の状態:
- `projection_body_rigid_enabled`
- `projection_hook_length_enabled`
- `projection_basal_link_direction_enabled`
- `projection_flagella_chain_length_enabled`
- `projection_flagella_template_enabled`

hook 幾何:
- `hook_len_mean_over_b`
- `hook_len_min_over_b`
- `hook_len_max_over_b`
- `hook_len_rel_err_mean`
- `hook_len_rel_err_max`

可能なら追加:
- `hook_angle_mean_deg`
- `hook_angle_min_deg`
- `hook_angle_max_deg`
- `hook_angle_err_mean_deg`
- `hook_angle_err_max_deg`

flagella 幾何:
- `flag_bond_len_mean_over_b`
- `flag_bond_len_min_over_b`
- `flag_bond_len_max_over_b`
- `flag_bond_rel_err_mean`
- `flag_bond_rel_err_max`

torsion / force 検証用:
- `F_torsion_mean_flag`
- `F_torsion_mean_body`
- `F_total_mean_flag`
- `F_total_mean_body`
- `torsion_fd_eps_m`

既存列がある場合は重複追加しないこと。  
既存命名との整合を優先すること。

### 重要
hook angle については **まず観測列を追加する**。  
ただし，現時点で論文由来の厳密 pass/fail しきい値が弱い場合は，**hard gate にしない**こと。

---

## D. 論文準拠の初期べん毛生成 mode を追加する

### 目的
初期べん毛形状と Normal 状態の平衡 bend / torsion が一致するようにし，`motor.torque_Nm=0` でも初期緩和反力で崩れない方向へ寄せる。

### 必須方針
- 既存の `helix_init.radius_over_b` / `pitch_over_b` ベース初期化は **legacy mode** として残してよい
- ただし，新たに **paper 準拠 mode** を追加すること
- `conf/sim_swim.yaml` の既定は，可能なら paper 準拠 mode に寄せること
- paper 準拠 mode では，初期べん毛形状を **target の `bond_L_over_b`, `theta0_deg.normal`, `phi0_deg.normal`, bead 数** に整合するよう生成すること

### config 例
命名は適宜調整してよいが，意図は以下を満たすこと。

- `flagella.init_mode: "paper_table1" | "legacy_radius_pitch"`
- `flagella.n_beads_per_flagellum`
- `flagella.bond_L_over_b`
- `potentials.bend.theta0_deg.normal`
- `potentials.torsion.phi0_deg.normal`

### 制約
- paper 準拠 mode では，`radius_over_b` / `pitch_over_b` を source of truth にしない
- 必要なら `radius/pitch` は builder 内で導出し，summary に出す
- source of truth が二重化する入力は避ける
- 初期化のために hidden correction を step 側へ持ち込まないこと

---

## E. 初期状態の幾何 summary を出力する

### 必須出力
以下のいずれか，または複数で初期状態を明示すること。
- `run.log`
- `manifest.json`
- `sim/initial_geometry_summary.json`
- `sim/initial_geometry_summary.csv`

### 少なくとも含める項目
全体:
- `flagella.init_mode`
- `n_flagella`
- `n_beads_per_flagellum`
- `bond_L_over_b`
- target `theta0_deg.normal`
- target `phi0_deg.normal`

各べん毛ごと:
- bead 数
- contour length
- end-to-end length
- `attach-first` 長さ
- mean/min/max bond length
- 派生 helix radius
- 派生 helix pitch
- initial hook angle
- initial bend angle mean/max
- initial torsion angle mean/max
- target との差（mean/max）
- initial tangent vs rear-direction angle

### 目的
- 実長さが分からない / 解釈が曖昧という問題を避ける
- 「初期形状が本当に target 平衡値に合っているか」をログから検証可能にする
- 後から run を見返したときに，bead 数・幾何・論文整合性を追跡可能にする

---

## F. torsion force 実装の検証

### Phase A（完了済みの記録）
- `compute_torsion_forces()` の有限差分幅 `fd_eps_m` について，少なくとも
  - 現状値
  - 現状の 1/100
  - 現状の 1/1000
  を比較した
- 比較条件は `paper_table1`・`n_flagella=1`・`motor.torque_Nm=0`・all projection OFF
- current candidate は **`fd_eps_over_b=1e-3`**
- `1e-4` まで下げても見た目上の改善は増えなかったため，現時点では `1e-3` を優先候補とする

### Phase B（次の必須検証）
- `paper_table1`・`n_flagella=1`・`motor.torque_Nm>0`・all projection OFF・`fd_eps_over_b=1e-3` で run する
- 少なくとも以下を比較すること
  - `F_torsion_mean_flag`
  - `F_torsion_mean_body`
  - `F_total_mean_flag`
  - `flag_torsion_err_mean_deg`
  - `flag_bond_len_mean_over_b`
  - `hook_len_mean_over_b`
  - `body_spring_max_stretch_ratio`
  - `body_bend_max_error_deg`
- 不明点や解釈が曖昧な場合は，この段階で事前に質問すること

### Phase C（条件付き）
- Phase B でなお torsion force が異常に大きい，または `motor ON` で不安定性が残る場合に限り，
  - `flag_torsion_stiffness_scale`
  - `flag_bend_stiffness_scale`
  の扱いを検討してよい
- ただし，scale 変更は「論文準拠 mode の default」を即変えるのではなく，
  - debug / legacy 専用に留める
  - paper mode では 1.0 を既定とすべきか
  - 完全に廃止すべきか
  まで含めて**結論を出す対象**とする

### 禁止
- Phase B を飛ばして `flag_torsion_stiffness_scale` を先に変更すること
- `fd_eps_m` 検証を省略して「scale を下げたら安定した」で終わらせること

---

## G. テスト方針

重い長時間シミュレーションをコミット前必須にしないこと。  
短時間・小規模・再現性の高いテストを整備すること。

### 1. 既存 commit 前必須 pytest は維持
既存で固定済みの以下は引き続き pass させること。

- `tests/test_body_stability.py`
- `tests/test_params.py`
- `tests/test_e2e_cli.py`

### 2. 今回の追加必須テスト
以下のような projection OFF 系テストを追加すること。

#### a. config / CLI で個別 ON/OFF が効く
- 新しい projection パラメータが YAML / CLI override から読み込まれる
- デフォルトは true
- false 指定時に false になる

#### b. projection OFF 時に本当にその projection を通っていない
少なくとも 1つは，呼び出しレベルまたは挙動レベルで，
- hook length projection
- basal link direction projection
- flagella chain projection
- flagella template projection
の各 bypass が機能していることを確認する。

#### c. 1 flagellum 条件で projection OFF でも step 更新が完走する
最小比較条件は `n_flagella=1` を使ってよい。  
純粋な body+hook-only 系は現在のトポロジでは定義しにくいので，**1 flagellum を最小検証系**とする。

#### d. `motor.torque_Nm=0` で，projection OFF 条件でも極端な幾何崩壊を起こさない
まずは 1 flagellum 条件で hard gate を置く。

#### e. `motor.torque_Nm>0` で，projection OFF 条件の diagnostics が出力される
ここは strict gate より，**比較可能な diagnostics が出ること**を重視する。

#### f. paper 準拠 mode で初期形状が target 平衡値と整合する
少なくとも `n_flagella=1` の builder / simulator 初期状態について，
- initial hook length
- initial flag bond length
- initial bend angle
- initial torsion angle

が target 値と整合することを確認するテストを追加すること。

#### g. 初期 geometry summary が出力される
- `run.log` または summary file に必要項目が出ること
- bead 数，contour length，target / measured bend-torsion が取得できること

#### h. Phase A の再現テスト
少なくとも `paper_table1`・`n_flagella=1`・`motor.torque_Nm=0`・all projection OFF で，
- `fd_eps_over_b=1e-3` では静止平衡に近いこと
- `fd_eps_over_b=0.1` との差が診断可能であること

を確認するテスト，または検証用テストを追加すること。

#### i. Phase B の比較が可能であること
- `motor ON, n_flagella=1, fd_eps_over_b=1e-3` の結果を追跡できる
- どの `fd_eps_m` で回したかを後から追跡できる

---

## 定量基準（hard gate と soft gate を分ける）

## Hard gate（根拠が比較的あるもの）
既存テスト・既存実装に寄せて，まず以下を commit 前基準候補とする。

### hard gate の適用範囲
hard gate の閾値は，まずは **`n_flagella=1` かつ `motor.torque_Nm=0` の追加テストにのみ適用**する。  
`motor.torque_Nm>0` および `n_flagella=3` は，現段階では比較可能性と diagnostics による原因説明を重視する **soft gate** として扱う。

### body
body-only / 低負荷条件では既存基準を維持すること。  
既存閾値と整合しない変更を入れる場合は理由を明記すること。

### hook length
`attach-first` の距離について，少なくとも 1 flagellum / torque=0 / 短時間条件では以下を目安にする。

- mean: rest length に対して **±5% 以内**
- min/max: rest length に対して **0.85〜1.15 倍** 以内

### flagella bond length
1 flagellum / torque=0 / 短時間条件で以下を目安にする。

- mean: rest length に対して **±2% 以内**
- min/max: rest length に対して **0.95〜1.05 倍** 以内

### initial-geometry hard gate
paper 準拠 mode では，少なくとも `n_flagella=1` の初期状態で，
- target と measured の bond 長さ誤差
- target と measured の bend 角誤差
- target と measured の torsion 角誤差

が十分小さいことを確認する。

### torsion-implementation hard gate
少なくとも `paper_table1`・`n_flagella=1`・`motor.torque_Nm=0`・all projection OFF の短時間条件で，
- `fd_eps_over_b=1e-3` が静止平衡を保つこと
- `fd_eps_over_b=0.1` との差が説明できること

を確認する。

### bend / torsion
既存テストにあるなら，1 flagellum 条件でその閾値を流用してよい。

---

## Soft gate（今回は観測中心）

### hook angle
- まず diagnostics に出す
- 3 flagella 条件での分布を見る
- しきい値を今PRで無理に固定しない

### 3 flagella 条件
- `n_flagella=3` は本命条件だが，まずは **diagnostics から原因候補を説明できること** を重視する
- 閾値が弱いのに hard gate を置かないこと

### legacy mode
legacy の `radius/pitch` ベース初期化は残してよいが，
- paper 準拠性は soft 扱い
- 解析や比較では paper 準拠 mode を優先する

### stiffness scale
- Phase C までは **即変更しない**
- 先に Phase B の結果を見る
- 最終的には「paper mode で使うべきか」を結論として残す

---

## 推奨する最小比較条件

これらの比較条件は，主に実装後の評価軸として用いる。  
Codex は比較可能な config・diagnostics・軽量 sanity test を整備することを主対象とし，重い本比較実行はユーザーが行う。

### Stage 0
- body-only
- これは既存 PR #32 の基盤を前提にする

### Stage 1
- `n_flagella=1`
- `motor.torque_Nm=0`
- body projection OFF
- hook / flagella projection の 2x2 比較

### Stage 1A（完了済み）
- `paper_table1`
- `n_flagella=1`
- `motor.torque_Nm=0`
- all projection OFF
- `fd_eps_m` sweep 比較
- current candidate: `fd_eps_over_b=1e-3`

### Stage 2（次の必須検証）
- `paper_table1`
- `n_flagella=1`
- `motor.torque_Nm>0`
- all projection OFF
- `fd_eps_over_b=1e-3`

### Stage 3
- `n_flagella=3`
- `motor.torque_Nm=0`
- body projection OFF
- hook / flagella projection の 2x2 比較

### Stage 4
- `n_flagella=3`
- motor ON
- body projection OFF
- hook / flagella projection の 2x2 比較

### 重要
`hook OFF / flagella ON` だけで主因を断定しないこと。  
`hook OFF / flagella OFF`，`hook ON / flagella OFF` を含めて解釈すること。  
また，不明点や結果の解釈が曖昧な場合は先に質問すること。

---

## model_alignment_notes.md の更新

`prompts/phase2/model_alignment_notes.md` は適切に更新すること。  
特に以下は記録対象。

- body rigid projection が論文外の補助であること
- hook / flagella projection が論文モデルより強い拘束だった可能性
- basal link direction projection の位置づけ
- 今回 distance-only 寄りに扱った箇所
- projection OFF 時に何を spring / bend / torsion に委ねたか
- hidden correction を入れていないこと
- 旧実装では，初期べん毛形状 (`radius/pitch`) と target bend/torsion が独立であり，初期 mismatch を生みうること
- `paper_table1` で初期 shape mismatch は大きく改善されたこと
- Phase A の結果，主因候補が `fd_eps_m` であること
- `fd_eps_over_b=1e-3` を current candidate とした理由
- `flag_torsion_stiffness_scale` / `flag_bend_stiffness_scale` が論文準拠 mode で必要かどうかに関する最終結論は Phase B/C 後に出すこと
- 新しい paper 準拠 mode で何を source of truth にしたか
- 派生値として何を算出するようにしたか
- 実装上まだ論文と一致しない点（あれば）
- legacy mode を残した理由
- まだ不十分な点 / 論文との差分

### 注意
ここは「何でも書く」のではなく，
- 論文モデルに反する項目
- 実装モデルの不十分な点
- 今回の解釈変更
を中心に整理すること。

---

## 禁止事項

- diagnostics の根拠なしに global stiffness を上げる
- hidden projection / hidden correction を再導入する
- test 閾値だけを緩めて通す
- 無関係な refactor を混ぜる
- projection OFF のはずなのに実質同じ補正を残す
- 物理モデル変更を notes に書かない
- 初期 shape mismatch を放置したまま，stiffness 緩和だけで安定化したことにする
- Phase B を飛ばして `flag_torsion_stiffness_scale` を先に変更する
- 不明点があるまま独断で Phase C へ進むこと

---

## 実装メモ

- 現在の hook は `attach-first spring + (attach, first, second) triplet` を基礎としている
- 今回は **hook モデル自体の再設計** はしない
- まずは projection を外したときに，既存 spring / hook / bend / torsion / repulsion だけでどこまで保てるかを見る
- body+hook-only を新規モデル化するより，まずは `n_flagella=1` を最小検証系とする
- body projection は PR #32 の成果を前提として使うが，今回の主対象は hook / flagella projection 側である
- ただし，projection 切り分けだけでは不十分であり，**paper 準拠の一意な初期化**と**torsion force 実装の検証**が必要である
- べん毛のビーズ本数や実長さの解釈が後から追跡できるように，初期 geometry summary は必須とする
- `flag_torsion_stiffness_scale` の扱いに関する最終結論は，Phase B/C 後に出す

---

## DoD

以下を満たしたら完了。

1. `main` から切った新ブランチ上で作業している
2. hook / flagella projection が個別 ON/OFF 可能
3. default は現状互換で true
4. projection OFF のときに本当に bypass される
5. `motor.torque_Nm=0` の 1 flagellum 条件で，projection OFF 系の短時間安定性を確認できる
6. `motor.torque_Nm>0` の条件でも，projection 組み合わせごとの差を diagnostics で比較できる
7. `n_flagella=3` 条件で，崩壊時に CSV / diagnostics から原因候補を説明できる
8. 既存 commit 前必須 pytest が pass
9. 新規テストが pass
10. `model_alignment_notes.md` が適切に更新されている
11. hidden correction を入れていない
12. 無関係な差分を混ぜていない
13. paper 準拠の初期べん毛生成 mode が追加されている，または同等の source-of-truth 一意化が達成されている
14. 初期 geometry summary が log / manifest / file のいずれかで確認できる
15. `n_flagella=1`, `motor.torque_Nm=0` の初期状態で，target bend/torsion と生成形状の整合性を確認するテストがある
16. Phase A の結果として，`fd_eps_over_b=0.1` では崩壊し，`1e-3` / `1e-4` では静止平衡が保たれることが記録されている
17. current candidate として **`fd_eps_over_b=1e-3`** が記録されている
18. 次の必須検証として **`motor ON, n_flagella=1, fd_eps_over_b=1e-3`** が実施され，結果が記録されている
19. `flag_torsion_stiffness_scale` / `flag_bend_stiffness_scale` を paper mode でどう扱うかに関する結論が，Phase B/C の結果にもとづいて残っている
20. 不明点や曖昧な点が出た場合に，事前質問した記録またはその対応方針が残っている

---

## 最後に
この Issue #33 の目的は，
**3本後方束化の最終達成ではなく，hook / flagella projection が崩壊や束化阻害の主因かどうかを切り分け，さらに初期べん毛形状と平衡 bend/torsion の不整合を解消し，残る torsion force 実装上の問題を検証すること**
である。

そのため，

- まず projection を分解し
- どこを切ると崩れるかを見える化し
- そのうえで「distance-only で十分か」「どの拘束が強すぎたか」を整理し
- **paper 準拠の一意な初期べん毛生成**と**初期 geometry summary の明示**を整え
- Phase A の結果を踏まえて **`fd_eps_over_b=1e-3` を current candidate とし**
- 次に **`motor ON, n_flagella=1`** を検証し
- 最後に **`flag_torsion_stiffness_scale` の扱いに結論を出す**

ことを優先する。