# 0033: projection 非依存の原因切り分けと，`n_flagella=1`・motor ON・all projection OFF 安定化に向けた実装修正

## 対象
- Repo: `Kenta-morimori/prj-flagella-estimation`
- Base branch: `main`
- このタスクでは **main から新規ブランチを切って作業**すること
- 対応 Issue: #33
- 想定タイトル: `[Phase2] stabilize n=1 motor-on without projection`

### 作業上の原則
- **不明点や曖昧な点・詰めるべき要件が見られる場合は事前に質問すること**
- 独断でタスク範囲を拡張しないこと
- 各フェーズの結論と根拠をログ・summary・notes のいずれかに残すこと
- **projection は最終解として採用しないこと**
- **projection は診断用比較のみに使い，本番モデル・比較評価は potential 主体で行うこと**
- **projection あり条件を安定性評価の主指標・相対評価の基準にしないこと**
- **新規比較・新規評価では projection default を OFF とすること**
- **相対評価の中心は all projection OFF 条件とすること**

---

## 背景

直前の PR #32 / Issue #29 では，body-only 条件を切り出して安定性を確認した。

- `projection.enable_body_rigid_projection` を追加し，body rigid projection を ON/OFF 可能にした
- body 側面長方形に **両対角 spring** を追加した
- `n_flagella=0` では，`projection.enable_body_rigid_projection=false` でも body は安定した
- 一方で `n_flagella=3` では body 崩壊が起きうるため，主因は body 単体ではなく **hook / flagella が body に返す負荷**の可能性が高い

ただし，ここで注意すべき点がある。  
`n_flagella=0` の body-only 安定は，**body が自発的に崩れにくい**ことは示すが，**motor ON 時に flagellum から返る実負荷に対して十分に安定**であることまでは保証しない。  
したがって，body-only 安定は必要な切り分けではあるが，**motor equivalent load に対する十分条件ではない**。

このため，次の切り分け対象として **hook / flagella projection 側** を扱った。

### 追加で今回分かったこと
PR #34 の Stage 1 (`n_flagella=1`, `motor.torque_Nm=0`) の 2x2 比較では，

- hook OFF / flagella OFF
- hook OFF / flagella ON
- hook ON / flagella OFF
- hook ON / flagella ON

の **全条件で形状崩壊**が生じた。

その後，`paper_table1`・`n_flagella=1`・`motor.torque_Nm=0`・all projection OFF で再検証したところ，

- 初期 bend / torsion / hook angle は target と実質一致
- `paper_table1` と all projection OFF は正しく反映
- `fd_eps_over_b=0.1` では step 0 から崩壊
- `fd_eps_over_b=1e-3` および `1e-4` では，菌体・べん毛とも静止平衡をほぼ維持

であった。

したがって，projection の有無そのものや，paper mode における初期 shape mismatch は主因ではない可能性が高い。  
Phase A の結論として，**torsion force 実装側，特に有限差分幅 `fd_eps_m` が大きすぎることが崩壊原因であり，current candidate は `fd_eps_over_b=1e-3`** とする。

一方で，`paper_table1`・`n_flagella=1`・all projection OFF・`fd_eps_over_b=1e-3` でも，

- `motor.torque_Nm = 4e-18`
- `motor.torque_Nm = 1e-18`
- `motor.torque_Nm = 5e-20`

のいずれでも `motor ON` では基部から崩壊した。  
このため，次の主因候補は **`compute_motor_forces()` による torque→force couple 分配**，または **motor 印加時の basal 幾何拘束不足** であると考えていた。

その後，Phase D / E と追加比較により，以下が分かった。

- `hook_length` により hook 長の崩れ自体は抑えられる
- `hook_length + basal_link_direction` でも flagella chain 長の崩れは抑えられない
- `chain_length` のみ ON では短時間安定
- `template` のみ ON でも短時間安定
- `hook_basal_ref` は即時破綻
- `Ta + Tb = Ttot` の split residual は機械誤差レベルであり，`compute_motor_forces()` の gross bug は主因ではなさそう

したがって，現時点では

- **projection は数値安定化には有効**
- しかし **論文の potential 主体モデルより強い人工拘束**
- 現実装では `motor ON` 時の flagella shape-maintenance が不足しており，`chain_length` または `template` がその不足を人工的に埋めている可能性が高い

と整理する。

さらに body-only の motor equivalent load 比較により，

- baseline / `attach_proxy` では body はほぼ無変形
- 旧 `pure_couple` では body の局所ねじれが発生
- 作用点・分配を見直した `pure_couple_distributed` では body-only short-time 条件で nearly rigid に近い応答へ改善

した。  
したがって，旧 pure couple の問題は **body 一般の不安定性よりも load distribution 起因**の可能性が高く，body-only proxy は `pure_couple_distributed` を採用する。

また，projection を比較にすら使わず原因切り分けしたいという方針に基づき，次段では

1. **菌体のみ**
2. **菌体 + minimal basal stub**
3. **菌体 + full flagella**

の3段階を **すべて projection OFF** で比較し，どの段階から破綻が現れるかを評価する。

ここで **minimal basal stub** とは，
- body attach bead
- first bead
- second bead  
までを残し，
- その先の helix chain を持たない最小基部系

を指す。  
これは「菌体 + フックのみ」を実装上明確に定義した比較系である。

最新の Phase G では，

- body-only は短時間で安定
- minimal basal stub は短時間完走するが，`local_attach_first_rel_err` が大きい
- full flagella では `local_first_torsion_err_deg` と `flag_bond_rel_err_max` が早期に立つ

ことが分かった。  
したがって現時点では，**basal/hook 近傍の局所 stretch 問題**と，**full chain/torsion 側の追加問題**の二層構造を仮説として保持する。

よって，この prompt では

1. projection の個別 ON/OFF 化
2. **新規比較・新規評価での projection default OFF 化**
3. **論文準拠の一意な初期べん毛生成**
4. **初期状態の幾何情報の明示的 logging / summary 出力**
5. **torsion force 実装（とくに `fd_eps_m`）の検証**
6. **motor diagnostics の追加と motor/basal 問題の切り分け**
7. **projection を不採用にした上で，body-only に motor equivalent load を与えた形状安定性検証**
8. **body-only / minimal basal stub / full flagella の3段階比較系整備**
9. **flagella 側局所破綻モードを特定するための diagnostics 追加実装**
10. **`n_flagella=1`, motor ON, all projection OFF の安定化**
11. **`flag_torsion_stiffness_scale` / `flag_bend_stiffness_scale` の扱いに関する結論整理**

を扱う。

---

## 現 `main` / 現ブランチの把握事項（前提）

実装前に必ず現コードを読んで確認すること。

### 1. 現在 config 化されている projection は body 側のみ
- `projection.enable_body_rigid_projection` は存在する
- hook / flagella projection を個別に切る config はまだない

### 2. `step()` の最後で hook / flagella 側 projection が常時走る
現 `main` では，body rigid projection の後に hook / flagella 側 projection が必ず適用される。

### 3. hook / flagella projection の中身は少なくとも以下に分かれている
- hook length の補正
- basal link direction の補正
- flagella chain length の補正
- flagella template の補正

### 4. hook の現在の物理定義
- `attach bead - first flag bead` は spring pair に入っている
- hook は `(attach, first, second)` の triplet で表されている
- motor triplet も同じ triplet を使っている

### 5. 初期化時の幾何
- `attach -> first` は body layer centroid から見た radial 方向に短く置かれている
- `first -> second` の基底接線は rear direction に概ね揃えられている
- これは **初期条件** であり，時間発展中もその方向を hard に固定すべきとは限らない

### 6. 現状の問題点
- 旧来の `legacy_radius_pitch` では，初期べん毛形状と target bend / torsion が独立であり，初期 mismatch を生みうる
- 一方，`paper_table1` では初期 shape mismatch は大きく改善された
- Phase A により，`fd_eps_m` が崩壊主因だったことが切れた
- current candidate は **`fd_eps_over_b=1e-3`**
- しかし `motor ON, n_flagella=1` では，`4e-18`, `1e-18`, `5e-20` のいずれでも崩壊した
- Phase D / E と追加比較により，`hook_basal` だけでは不足し，`chain_length` または `template` が入ると短時間安定することが分かった
- body-only proxy 比較により，旧 `pure_couple` は不適切な load distribution であり，`pure_couple_distributed` が妥当な proxy と分かった
- minimal basal stub / full flagella 比較により，現状は  
  **どこが最初に壊れるかの指標は整理できたが，安定化自体はまだ未達**
- したがって，現時点の主因候補は **gross な motor split bug** ではなく，**motor ON 時に potential のみで flagella shape を保てないこと** にある
- engine 内には `flag_bend_stiffness_scale = 300`，`flag_torsion_stiffness_scale = 300`，`body_stiffness_scale = 50` があり，これらは論文の一次入力ではなく repo 独自の補助係数である可能性が高い
- ただし，現時点では **body 一般の剛性不足を先に疑う段階ではなく，basal/hook 近傍修正と full chain/torsion 修正を優先**すべきである

---

## このタスクの目的

### 第1目標
`motor.torque_Nm=0` で，**hook / flagella projection を切っても**，body / hook / flagella が直ちに崩壊しないことを確認できる状態にする。

### 本PRの最終目標
`paper_table1`, `n_flagella=1`, `motor.torque_Nm=5e-20`, `fd_eps_over_b=1e-3`, all projection OFF の条件で，**potential のみ**で短時間窓において幾何崩壊せず、有限値で完走し、局所指標も許容範囲内に保つこと。

### このPRで達成しないこと
- 3本べん毛が菌体後方で束化し，推進する最終 run 状態の達成
- projection を本番拘束として採用すること
- body-only モデルの全面再設計
- 無関係な refactor
- hidden projection / hidden correction の再導入
- 初期 mismatch を stiffness 緩和だけで見かけ上隠すこと
- `flag_torsion_stiffness_scale` を根拠なく即変更すること
- projection あり条件を安定性の合格判定に使うこと

---

## 今回の設計方針（重要）

### 方針 1: hook は「角度固定」ではなく「距離中心」で扱う
論文モデル寄りに，hook については以下を採ること。

- **常時の角度固定は行わない**
- `attach-first` の距離は **projection ではなく spring** を主拘束とする
- hook angle は，body 交差を防ぐための **one-sided な hook 制約**として扱うのは可
- `basal link direction` の hard projection は，論文より強い拘束の可能性があるため，個別に ON/OFF 可能にする

### 方針 2: hook と flagella の projection はまとめてではなく分解する
最低限，以下を個別に ON/OFF 可能にすること。

- `projection.enable_hook_length_projection`
- `projection.enable_basal_link_direction_projection`
- `projection.enable_flagella_chain_length_projection`
- `projection.enable_flagella_template_projection`

### 方針 3: projection の default を OFF にする
- **新規比較・新規評価では projection default を OFF とする**
- 既存互換 run が必要な場合のみ，明示的に ON を指定する
- 既存テスト・既存出力に影響する場合は，互換性維持の扱いを `run.log` / notes に明記する

### 方針 4: projection は診断用に限定する
- `chain_length` と `template` は **数値安定化には有効**
- しかし **論文モデルの potential 主体実装より強い人工拘束**
- したがって，**これらを最終解として採用しないこと**
- 役割は **どの破綻 mode を抑えているかを診断するための比較用拘束** に限定すること
- **projection あり条件を安定性評価の主指標・相対比較の基準にしないこと**
- **相対評価の中心は all projection OFF とすること**

### 方針 5: 比較は projection ではなく系の構成で段階化する
projection 比較ではなく，以下の3段階比較を主軸にする。

1. **body-only**
2. **body + minimal basal stub**
3. **body + full flagella**

ここで、
- body-only は `n_flagella=0` と body-equivalent-load proxy を使う
- minimal basal stub は attach / first / second のみを残す最小基部系
- full flagella は通常の `n_flagella=1` paper_table1 系

とする。

### 方針 6: first-fail を特定してから安定化する
安定化修正の前に、まず **first-fail 判定指標** を固定する。

- 一次指標: `local_attach_first_rel_err`
- 補助指標:
  - `local_first_second_rel_err`
  - `local_second_third_rel_err`
  - `local_basal_bend_err_deg`
  - `local_first_torsion_err_deg`
  - `flag_bond_rel_err_max`

現時点では、
- minimal basal stub でも `local_attach_first_rel_err` が大きい
- full flagella では `local_first_torsion_err_deg` と `flag_bond_rel_err_max` が追加で強く立つ

ため、まず **basal/hook 近傍の local stretch 問題**を先に潰し、その後 **full chain/torsion 側** を詰める方針とする。

### 方針 7: 初期べん毛は「論文の離散モデル量」を正とする
初期べん毛形状は，`helix_init.radius_over_b` と `helix_init.pitch_over_b` を一次入力とするのではなく，**論文 Table 1 に対応する離散モデル量**を source of truth とすること。

少なくとも以下を一次入力候補として扱う。

- `bond_L_over_b`
- `potentials.bend.theta0_deg.normal`
- `potentials.torsion.phi0_deg.normal`
- `n_beads_per_flagellum`
- `n_flagella`

`radius_over_b` と `pitch_over_b` は，必要なら **派生値として計算・記録**する。  
paper 準拠 mode では，`radius/pitch` を手入力で同時指定して source of truth を二重化しないこと。

### 方針 8: 初期状態の情報を log / summary に明示する
実長さの解釈や bead 数の妥当性を後から確認できるよう，初期状態について以下を必ず記録すること。

- 各べん毛の bead 数
- 各べん毛の contour length / end-to-end length
- `attach-first` 長さ
- flagella bond 長さ統計
- 初期 hook angle
- 初期 bend / torsion の実測値と target 値との差
- 派生された helix radius / pitch
- 初期 tangent と rear direction の角度
- 使用した init mode と source-of-truth 項目

### 方針 9: torsion force の問題はフェーズを分けて扱う
このタスクでは，以下の順で扱うこと。

#### Phase A（完了済み）
- **`fd_eps_m` を 100〜1000 倍小さくする sweep** を行った
- `paper_table1`・`n_flagella=1`・`motor.torque_Nm=0`・all projection OFF で再検証した
- `fd_eps_over_b=0.1` では崩壊
- `fd_eps_over_b=1e-3` および `1e-4` では静止平衡を維持
- current candidate は **`fd_eps_over_b=1e-3`**

#### Phase B（完了済み）
- `paper_table1`・`n_flagella=1`・`motor.torque_Nm>0`・all projection OFF・`fd_eps_over_b=1e-3` を検証した
- `motor.torque_Nm = 4e-18`, `1e-18`, `5e-20` のいずれでも崩壊した
- したがって，**torsion finite-difference 問題は切れたが，motor ON の不安定性は未解決**である

#### Phase C（完了済み）
- **motor diagnostics を追加**し，step 0 / 1 の basal triplet まわりを可視化できるようにした
- `paper_table1`・`n_flagella=1`・`motor.torque_Nm=5e-20`・`fd_eps_over_b=1e-3` を基準条件とした

#### Phase D / E（完了済み）
- `hook_basal` だけでは十分でないことを確認
- split residual は gross bug を支持しない
- 形状保持不足が主因候補として残った

#### Phase F（完了済み）
- body-only proxy を `pure_couple_distributed` で整備
- body 一般の剛性不足は主因候補から後退

#### Phase G（完了済み）
- minimal basal stub 実装
- local breakdown diagnostics 実装
- 0.01 s / 0.05 s の追試により  
  **basal/hook 近傍の局所 stretch 問題 + full chain/torsion 側の追加問題**  
  の二層構造を仮説として整理

#### Phase H（今回の次アクション）
- first-fail 指標を固定
- `local_attach_first_rel_err` の意味を最終確認
- **basal/hook 近傍の local stretch 問題を先に修正**
- その後 **full chain/torsion 側**の修正へ進む
- 各修正後に `n_flagella=1`, `motor ON`, all projection OFF で再検証する

### 方針 10: 追加 scale は論文値ではない前提で扱う
- `flag_bend_stiffness_scale`
- `flag_torsion_stiffness_scale`
- `body_stiffness_scale`

は論文の一次入力そのものではなく，repo 独自の補助係数とみなすこと。  
したがって，paper mode ではその使用可否・既定値を慎重に扱うこと。

---

## 実装要件

## A. config / params の追加
`ProjectionParams` に以下を追加すること。

- `enable_hook_length_projection: bool = False`
- `enable_basal_link_direction_projection: bool = False`
- `enable_flagella_chain_length_projection: bool = False`
- `enable_flagella_template_projection: bool = False`

### 注意
- **新規比較・新規評価では default OFF**
- 既存互換 run が必要なら，専用 config / preset で ON を明示すること
- 互換性に影響がある場合は `run.log` と notes に明記すること

必要箇所:
- `src/sim_swim/sim/params.py`
- config YAML
- CLI override
- 必要なら summary / manifest 系の出力

既存の
- `projection.enable_body_rigid_projection`
はそのまま維持すること。

---

## B. engine の projection 適用を分解する
現状の一括処理を整理し，どの projection が有効かで分岐させること。

### 必須
- hook length projection を単独で切れること
- basal link direction projection を単独で切れること
- flagella chain length projection を単独で切れること
- flagella template projection を単独で切れること

### 禁止
- projection OFF のときに，別名の hidden correction を裏で走らせること
- OFF なのに内部で同等の位置補正を行うこと
- config 名だけ追加して実際には bypass されないこと

---

## C. diagnostics を既存 CSV に追加する
新規 CSV を増やすより，既存の `step_summary.csv` や既存 diagnostics に列追加する方針で進めること。  
ただし既存ファイル名が不適切なら修正してよい。その場合は理由を明記すること。

### 最低限追加したい列
projection の状態:
- `projection_body_rigid_enabled`
- `projection_hook_length_enabled`
- `projection_basal_link_direction_enabled`
- `projection_flagella_chain_length_enabled`
- `projection_flagella_template_enabled`

hook 幾何:
- `hook_len_mean_over_b`
- `hook_len_min_over_b`
- `hook_len_max_over_b`
- `hook_len_rel_err_mean`
- `hook_len_rel_err_max`

flagella 幾何:
- `flag_bond_len_mean_over_b`
- `flag_bond_len_min_over_b`
- `flag_bond_len_max_over_b`
- `flag_bond_rel_err_mean`
- `flag_bond_rel_err_max`

torsion / force 検証用:
- `F_torsion_mean_flag`
- `F_torsion_mean_body`
- `F_total_mean_flag`
- `F_total_mean_body`
- `torsion_fd_eps_m`

motor diagnostics 用:
- `F_motor_mean_flag`
- `F_motor_mean_body`
- `motor_ra_len_um`
- `motor_rb_len_um`
- `motor_Ta_norm`
- `motor_Tb_norm`
- `motor_Fa_norm`
- `motor_Fb_norm`
- `motor_axis_vs_rear_direction_angle_deg`
- `motor_attach_force_norm`
- `motor_first_force_norm`
- `motor_second_force_norm`
- `motor_degenerate_axis_count`
- `motor_split_rank_deficient_count`
- `motor_bond_length_clipped_count`

body motor-equivalent test 用:
- `F_body_equiv_load_mean`
- `F_body_equiv_load_max`
- `body_equiv_load_mode`
- `body_equiv_load_target_torque_Nm`
- `body_equiv_load_target_force_N`

#### Phase H で使う局所 diagnostics（必須）
以下は **first-fail 判定と安定化再検証の必須指標** とする。

##### 一次指標
- `local_attach_first_rel_err`

##### 補助指標
- `local_first_second_rel_err`
- `local_second_third_rel_err`
- `local_basal_bend_err_deg`
- `local_first_torsion_err_deg`
- `flag_bond_rel_err_max`

##### 局所 force 分解
- `local_F_spring_attach_first`
- `local_F_spring_first_second`
- `local_F_spring_second_third`
- `local_F_bend_basal`
- `local_F_torsion_first`
- `local_F_motor_attach`
- `local_F_motor_first`
- `local_F_motor_second`
- `local_F_repulsion_basal_region`

##### 追加条件
- 上記は **step ごと** に出力できること
- 少なくとも `n_flagella=1`, `motor.torque_Nm=5e-20`, all projection OFF 条件で取得できること
- 可能なら新規 CSV `local_flagella_breakdown.csv` 等に分離してよい
- ただし命名と用途を明記し，`run.log` と manifest から追跡可能にすること

---

## D. 論文準拠の初期べん毛生成 mode を追加する
### 目的
初期べん毛形状と Normal 状態の平衡 bend / torsion が一致するようにし，`motor.torque_Nm=0` でも初期緩和反力で崩れない方向へ寄せる。

### 必須方針
- 既存の `helix_init.radius_over_b` / `pitch_over_b` ベース初期化は **legacy mode** として残してよい
- ただし，新たに **paper 準拠 mode** を追加すること
- `conf/sim_swim.yaml` の既定は，可能なら paper 準拠 mode に寄せること
- paper 準拠 mode では，初期べん毛形状を **target の `bond_L_over_b`, `theta0_deg.normal`, `phi0_deg.normal`, bead 数** に整合するよう生成すること

### config 例
- `flagella.init_mode: "paper_table1" | "legacy_radius_pitch"`
- `flagella.n_beads_per_flagellum`
- `flagella.bond_L_over_b`
- `potentials.bend.theta0_deg.normal`
- `potentials.torsion.phi0_deg.normal`
- `flagella.stub_mode: none | minimal_basal_stub | full_flagella`

### 制約
- paper 準拠 mode では，`radius_over_b` / `pitch_over_b` を source of truth にしない
- 必要なら `radius/pitch` は builder 内で導出し，summary に出す
- source of truth が二重化する入力は避ける
- 初期化のために hidden correction を step 側へ持ち込まないこと

---

## E. 初期状態の幾何 summary を出力する
### 必須出力
`run.log` は必須成果物として出力すること。加えて，以下のいずれか，または複数で初期状態を明示すること。
- `manifest.json`
- `sim/initial_geometry_summary.json`
- `sim/initial_geometry_summary.csv`

### 少なくとも含める項目
全体:
- `flagella.init_mode`
- `flagella.stub_mode`
- `n_flagella`
- `n_beads_per_flagellum`
- `bond_L_over_b`
- target `theta0_deg.normal`
- target `phi0_deg.normal`

各べん毛ごと:
- bead 数
- contour length
- end-to-end length
- `attach-first` 長さ
- mean/min/max bond length
- 派生 helix radius
- 派生 helix pitch
- initial hook angle
- initial bend angle mean/max
- initial torsion angle mean/max
- target との差（mean/max）
- initial tangent vs rear-direction angle

---

## F. 残タスク（新 DoD に対する順序）
1. **first-fail の厳密特定**
2. **`local_attach_first_rel_err` の意味確認**
3. **basal/hook 近傍の local stretch 問題の実装修正**
4. **full chain/torsion 側の実装修正**
5. **安定化判定閾値の設定**
6. **`n_flagella=1`, motor ON, all projection OFF で再検証**
7. **DoD 達成確認**
8. **`flag_torsion_stiffness_scale` / `flag_bend_stiffness_scale` の最終結論整理**

---

## G. テスト方針
### 既存 commit 前必須 pytest は維持
- `tests/test_body_stability.py`
- `tests/test_params.py`
- `tests/test_e2e_cli.py`

### 今回の追加必須テスト
- projection default が OFF になっていること
- CLI / YAML で projection ON に戻せること
- `minimal_basal_stub` 初期化ができること
- `minimal_basal_stub` で短時間 run が完走すること
- `full_flagella` で局所 breakdown CSV が出ること
- attach-first / first-second / second-third / basal bend / first torsion / local force 分解が取得できること
- `n_flagella=1`, motor ON, all projection OFF の再検証が機械的に実行できること

---

## H. 定量基準（hard gate と soft gate を分ける）
## Hard gate
- `n_flagella=1`, `motor.torque_Nm=0`, all projection OFF
- `n_flagella=0`, body-only proxy
- `minimal_basal_stub` の短時間完走

## Soft gate
- `n_flagella=1`, `motor.torque_Nm>0`, all projection OFF（最終 DoD の達成前は soft）
- projection ON 条件
- 3 flagella 条件

### projection
- projection 比較は診断に使ってよい
- ただし **本番モデルの採用基準にしない**
- **安定性評価の主指標・相対比較の基準にしない**
- 比較結果は「何を projection が補っているか」の説明に限定する

---

## I. 推奨する最小比較条件
### Stage 0
- body-only

### Stage 1
- `n_flagella=1`
- `motor.torque_Nm=0`
- body projection OFF
- hook / flagella projection の 2x2 比較

### Stage 1A（完了済み）
- `paper_table1`
- `n_flagella=1`
- `motor.torque_Nm=0`
- all projection OFF
- `fd_eps_m` sweep 比較

### Stage 2（完了済み）
- `paper_table1`
- `n_flagella=1`
- `motor.torque_Nm>0`
- all projection OFF
- `fd_eps_over_b=1e-3`

### Stage 3（完了済み）
- basal 最小比較
  - all OFF
  - `hook_length` のみ ON
  - `hook_length + basal_link_direction` ON
  - all ON

### Stage 4（完了済み）
- `chain_only`
- `template_only`

### Stage 5（完了済み）
- body-only
- old `pure_couple`
- `pure_couple_distributed`
- `attach_proxy`

### Stage 6（完了済み）
- **body-only**
- **body + minimal basal stub**
- **body + full flagella**
- すべて **projection OFF**
- `motor.torque_Nm=5e-20`
- `fd_eps_over_b=1e-3`
- 局所 breakdown diagnostics 実装
- first-fail 判定指標の暫定整理

### Stage 7（今回の本命）
- `n_flagella=1`, `motor.torque_Nm=5e-20`, `fd_eps_over_b=1e-3`, all projection OFF
- **basal/hook 近傍の local stretch 問題を修正**
- 修正後に再実行し，一次指標 `local_attach_first_rel_err` が改善するか確認

### Stage 8
- Stage 7 の改善後，
- **full chain/torsion 側** を修正
- `local_first_torsion_err_deg`, `flag_bond_rel_err_max` の改善を確認

### Stage 9
- 安定化判定閾値を暫定設定
- `n_flagella=1`, motor ON, all projection OFF で DoD 判定

---

## model_alignment_notes.md の更新
特に以下は記録対象。

- projection default を OFF にしたこと
- projection を安定性評価の主指標にしないこと
- body-only proxy は `pure_couple_distributed` を採用したこと
- old `pure_couple` が不適切な load distribution だった可能性
- 次段では projection 比較ではなく  
  **body-only / minimal basal stub / full flagella**  
  の3段階比較へ移ること
- minimal basal stub が壊れるかどうかで basal/hook vs helix chain を切る方針
- latest result として  
  **basal/hook 近傍の局所 stretch 問題 + full chain/torsion 側の追加問題**  
  の二層構造を保持すること
- `fd_eps_over_b=1e-3` は current candidate
- `flag_bend_stiffness_scale` / `flag_torsion_stiffness_scale` はまだ触らない

---

## 禁止事項
- diagnostics の根拠なしに global stiffness を上げる
- hidden projection / hidden correction を再導入する
- projection ON を評価の主基準に使う
- `minimal_basal_stub` の定義を曖昧にしたまま比較する
- body-only proxy を飛ばして，いきなり stiffness scale を先に変更する
- `flag_bend_stiffness_scale` / `flag_torsion_stiffness_scale` を first-fail 未確定のまま先にいじる
- 不明点があるまま独断で次フェーズへ進むこと

---

## 実装メモ
- `minimal_basal_stub` は **hook only** の曖昧さを避けるための実装定義である
- attach / first / second までを残し，その先の helix chain を持たない
- body-only → minimal basal stub → full flagella の順に比較することで，projection に頼らず原因切り分けする
- projection は今後も診断専用であり，安定性評価の主指標としては扱わない
- 次の主戦場は **`n_flagella=1` motor ON all projection OFF の安定化** である

---

## DoD
1. projection default が OFF
2. 既存互換 run では明示的に ON を指定できる
3. hook / flagella projection が個別 ON/OFF 可能
4. projection OFF のときに本当に bypass される
5. `paper_table1` 初期化が維持される
6. `fd_eps_over_b=1e-3` が current candidate として記録される
7. body-only proxy として `pure_couple_distributed` を採用した理由が記録される
8. `minimal_basal_stub` が実装される
9. body-only / minimal basal stub / full flagella の3段階比較が all projection OFF で実行できる
10. Stage 6 用局所 diagnostics が追加実装される
11. projection あり条件を安定性評価の主指標に使わない方針が記録される
12. `local_attach_first_rel_err` を一次指標とする first-fail 判定が整理されている
13. `local_attach_first_rel_err` の意味が確認されている
14. basal/hook 近傍の local stretch 問題に対する修正が実装されている
15. full chain/torsion 側に対する修正が実装されている
16. `paper_table1`, `n_flagella=1`, `motor.torque_Nm=5e-20`, `fd_eps_over_b=1e-3`, all projection OFF の条件で短時間窓を幾何崩壊せず有限値で完走する
17. 上記条件で一次・補助局所指標が暫定許容範囲内に収まる
18. `flag_torsion_stiffness_scale` / `flag_bend_stiffness_scale` はこの段階で不必要に変更していない
19. `run.log` / summary / diagnostics から比較条件を追跡できる
20. 不明点が出た場合に事前質問した記録または対応方針が残る

---

## 最後に
この Issue #33 の目的は，

**3本後方束化の最終達成ではなく，projection に頼らず，body-only / minimal basal stub / full flagella の3段階比較と局所 diagnostics によって `n_flagella=1`, motor ON, all projection OFF を安定化する道筋を作ること**

である。

そのため，

- projection は診断専用とし
- default を OFF にし
- body-only proxy は `pure_couple_distributed` を採用し
- 次に `minimal_basal_stub` を用いて basal/hook 近傍の local stretch 問題を詰め
- その後 full flagella で full chain/torsion 側を詰め
- 最後に `n_flagella=1`, motor ON, all projection OFF の安定化を達成する

ことを優先する。